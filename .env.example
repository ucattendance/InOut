# Copy to .env for local development (optional)
PORT=3000

# ESLint overlay off in dev (optional)
# ESLINT_NO_DEV_ERRORS=true

# Leave unset to use npm start proxy → Linode (recommended).
# Set only to call API directly (e.g. production build or override):
# REACT_APP_API_URL=https://api.inout.urbancode.tech
